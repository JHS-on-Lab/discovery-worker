# ================================================================
# 개발 서버 (Ubuntu) — APP_ENV=dev
# ================================================================

# RDS — 서버가 VPC 내부이므로 직접 접속
RDS_HOST=dev-shared-rds.cdaao0ik8058.ap-northeast-2.rds.amazonaws.com
RDS_PORT=3306
RDS_USER=admin
RDS_PASSWORD=thswhdgus1!
RDS_DB=keyword_collector

TUNNEL_ENABLED=false

# WORKER_ID 는 컨테이너 실행 시 주입한다 (예: docker run -e WORKER_ID=dev-1).